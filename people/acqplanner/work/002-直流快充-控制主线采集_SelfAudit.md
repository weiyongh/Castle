---
author: Planner
version: 0.2
create_time: 2026-09-17 01:09 +0800
update_time: 2026-09-17 01:20 +0800
status: TO_AUDIT
self_audit_time: 2026-09-17 01:20 +0800
---

# 002-直流快充-控制主线采集 SelfAudit

## 依据

- L3 Knowledge `新能源汽修L3学习-直流快充`
- `doc/采集输出规范.md`

## SelfCheck

- `python3 -m unittest discover -s src/tests`：PASS（11 tests）。
- `python3 src/acquisition.py check work/002-直流快充-控制主线采集.source.json --output-dir work`：PASS。
- 确定性生成、UTF-8/LF、时间递增、第 9 列对齐、`00s` 边界及末尾 `停止采集`：PASS。

## 内容自审

1. **目的覆盖：PASS**。时间线覆盖未连接、车桩连接、通信与充电发起、功率建立、稳态快充、正常停止、安全解锁和断开连接。
2. **步骤必要性：PASS**。只保留区分直流快充控制主线各阶段所需的状态和动作，未主动制造故障或扩展保护分支。
3. **现场可执行性：PASS**。各时点只安排一个主要动作，自动发起充电时明确不重复操作，全流程可由一人顺序完成。
4. **时序：PASS**。保留 15 秒未连接基线、240 秒连接、通信与功率建立窗口、120 秒稳态窗口、180 秒停止退出窗口及 90 秒拔枪后窗口；说明文档允许按实际状态整体顺延。
5. **分析线索：PASS**。插枪、发起充电、稳态、停止、解锁和拔枪均有独立时点，可用于后续对齐状态变化。
6. **内容边界：PASS**。输出使用车型无关的功能表达，未加入车企、品牌、具体车型、品牌 App 或专属界面路径。

## 结果

SelfCheck 与 SelfAudit 通过，当前版本可提交 Independent Audit。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 1

根据最新脚本规范修订为版本 0.2：

- `00s` 恢复为 `开始采集`，所有初始状态和采集前说明放在其缩进行中；
- 播报文本简化为 `插枪`、`启动充电`、`停止充电`、`解锁`、`拔枪` 等直接动作；
- 新增 `拍摄充电数据` 和 `拍摄停止状态` 两个明确取证事件。

## SelfAudit 2

1. **目的覆盖：PASS**。除完整快充控制主线外，稳态照片保留界面可见的充电状态、电压、电流、功率和 SOC，停止照片保留已停止且仍连接的现场状态。
2. **步骤必要性：PASS**。两个取证点分别服务于快充 Actual 功率建立和充电退出确认，未增加与目的无关的操作。
3. **现场可执行性：PASS**。每个时点只有一个主要动作，取证文本听后可直接执行；自动发起充电时不重复操作。
4. **时序：PASS**。拍摄位于稳态充电窗口和停止后的连接保持窗口，不与插枪、启动、停止、解锁或拔枪挤在同一时点。
5. **分析线索：PASS**。照片与 CAN 时间线共用明确时点，为区分 Capability、Request 和 Actual 提供可见 Actual 的同时刻参考，但不把显示值视为内部 Signal 的直接证明。
6. **内容边界：PASS**。照片只证明显示界面上可见的状态和数值，不用其替代通信、高压路径或接触器 Evidence；未引入特定品牌或界面路径。

SelfCheck 与 SelfAudit 2 通过，版本 0.2 可提交 Independent Audit。本结果不代表 `CONFIRM` 或 `PUBLISH`。
