---
author: Planner
version: 1.0
create_time: 2026-09-17 01:09 +0800
update_time: 2026-09-17 12:50 +0800
status: CONFIRM
self_audit_time: 2026-09-17 12:45 +0800
---

# 001-慢充-控制主线采集 SelfAudit

## 依据

- `doc/交流慢充L3控制树采集.md`
- L3 Knowledge `新能源汽修L3学习-交流慢充`
- `doc/采集输出规范.md`

## SelfCheck

- `python3 -m unittest discover -s src/tests`：PASS（11 tests）。
- `python3 src/acquisition.py check work/001-慢充-控制主线采集.source.json --output-dir work`：PASS。
- 确定性生成、UTF-8/LF、时间递增、第 9 列对齐、`00s` 边界及末尾 `停止采集`：PASS。

## 内容自审

1. **目的覆盖：PASS**。时间线覆盖未连接、接口建立、充电建立、稳态充电、正常停止、安全解锁和断开连接。
2. **步骤必要性：PASS**。只保留区分慢充控制主线各阶段所需的状态和动作，未主动制造故障或扩展保护分支。
3. **现场可执行性：PASS**。各时点只安排一个主要动作，操作和等待可由一人顺序完成。
4. **时序：PASS**。保留 15 秒未连接基线、120 秒充电建立窗口、120 秒稳态窗口、90 秒停止退出窗口及 60 秒拔枪后窗口；说明文档允许按实际状态整体顺延。
5. **分析线索：PASS**。插枪、稳态、停止、解锁和拔枪均有独立时点，可用于后续对齐状态变化。
6. **内容边界：PASS**。输出使用车型无关的功能表达，未加入车企、品牌、具体车型、品牌 App 或专属界面路径。

## 结果

SelfCheck 与 SelfAudit 通过，当前版本可提交 Independent Audit。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 1

根据最新脚本规范修订为版本 0.2：

- `00s` 恢复为 `开始采集`，所有初始状态和采集前说明放在其缩进行中；
- 播报文本简化为 `插枪`、`停止充电`、`解锁`、`拔枪` 等直接动作；
- 新增 `拍摄充电数据` 和 `拍摄停止状态` 两个明确取证事件。

## SelfAudit 2

1. **目的覆盖：PASS**。除完整慢充控制主线外，稳态照片保留界面可见的充电状态、电压、电流、功率和 SOC，停止照片保留已停止且仍连接的现场状态。
2. **步骤必要性：PASS**。两个取证点分别服务于功率建立和充电退出确认，未增加与目的无关的操作。
3. **现场可执行性：PASS**。每个时点只有一个主要动作，取证文本听后可直接执行。
4. **时序：PASS**。拍摄位于稳态充电窗口和停止后的连接保持窗口，不与插枪、停止、解锁或拔枪挤在同一时点。
5. **分析线索：PASS**。照片与 CAN 时间线共用明确时点，可为实际功率建立和退出提供同时刻现场旁证。
6. **内容边界：PASS**。照片只证明显示界面上可见的状态和数值，不用其替代 CAN 中的内部控制 Evidence；未引入特定品牌或界面路径。

SelfCheck 与 SelfAudit 2 通过，版本 0.2 可提交 Independent Audit。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 2

根据 Independent Audit 1 的 Finding 修订为版本 0.3：

- 将 `270s` 事件文本由 `停止充电` 改为 `车辆侧停止充电`；
- 固定本轮只使用车辆支持的正常方式发起停止；
- 明确不使用充电设施侧停止操作，使停止请求来源与 CAN 时间线保持可辨认。

## SelfAudit 3

1. **目的覆盖：PASS**。正常慢充主线保持完整，停止阶段明确覆盖车辆侧停止请求及其后的功率和状态退出。
2. **步骤必要性：PASS**。只明确原有停止动作的触发来源，没有增加与目的无关的状态或操作。
3. **现场可执行性：PASS**。事件播报直接说明从车辆侧停止，缩进说明给出车型无关的执行边界。
4. **时序：PASS**。停止动作仍位于 `270s`，与停止状态取证、解锁和拔枪保持独立时点。
5. **分析线索：PASS**。`车辆侧停止充电` 的事件时间可直接用于对齐车辆侧停止请求、充电功率下降和充电状态退出。
6. **内容边界：PASS**。未指定品牌、车型、App 或界面路径；未把现场操作本身当作内部 Signal 已成立的证明。

SelfCheck 与 SelfAudit 3 通过，版本 0.3 具备再次提交 Independent Audit 的条件。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 3

版本 0.3 通过 Independent Audit 后，根据现场执行完整性继续修订为版本 0.4：

- 在插枪后增加 `启动充电`，已经自动启动时不重复操作，未启动时从充电设施正常启动；
- 停止充电不再固定车辆侧，允许使用现场可用的车辆侧或充电设施侧正常方式；
- 要求记录实际启动和停止方式，避免现场操作来源丢失。

## SelfAudit 4

1. **目的覆盖：PASS**。主线同时覆盖自动启动和需要人工启动的正常慢充场景，并保留实际启动、停止来源。
2. **步骤必要性：PASS**。新增启动事件用于避免插枪后未自动充电造成流程中断；操作记录用于支持后续识别控制来源。
3. **现场可执行性：PASS**。自动启动时跳过重复操作，未自动启动时明确从充电设施启动；停止时可使用现场确实可用的正常方式。
4. **时序：PASS**。插枪后留有 30 秒判断自动启动，充电稳定后再取证；停止完成后才拍摄停止状态、解锁和拔枪。
5. **分析线索：PASS**。现场记录能够区分自动或人工启动，以及车辆侧或充电设施侧停止。
6. **内容边界：PASS**。操作均使用车型无关表达，不指定品牌、车型、App 或界面路径。

SelfCheck 与 SelfAudit 4 通过，版本 0.4 具备提交 Independent Audit 的条件。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 4

根据 Independent Audit 3 的 Audit Finding 2 修订为版本 0.5：

- 明确现场记录是与本次 CAN 文件同名关联的纯文本文件；
- 启动和停止时分别写入实际采集秒数及操作来源；
- 停止采集时要求将现场记录与 CAN 文件一起保存和交付。

## SelfAudit 5

1. **目的覆盖：PASS**。除完整慢充主线外，实际启动和停止来源形成可交付记录。
2. **步骤必要性：PASS**。只增加两行简短记录，不增加车辆操作或改变控制过程。
3. **现场可执行性：PASS**。执行者只需在启动和停止时各填写一次实际秒数和选项。
4. **时序：PASS**。记录发生在对应操作时点；时间顺延时填写实际秒数，仍可与 CAN 对齐。
5. **分析线索：PASS**。现场记录与 CAN 文件通过文件名关联，并随 CAN 一起保存交付。
6. **内容边界：PASS**。记录只描述现场实际操作，不推断内部 Signal 或诊断结论。

SelfCheck 与 SelfAudit 5 通过，版本 0.5 具备再次提交 Independent Audit 的条件。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 5

版本 0.5 通过 Independent Audit 后，为只听播报的现场执行者增强第一行提示，修订为版本 0.6：

- 增加 `新建现场记录` 和 `保存现场记录` 两个播报事件；
- 启动和停止播报直接提醒记下操作方式；
- 拍照播报明确包含充电状态和数据；
- `解锁` 改为 `解锁充电口`。

## SelfAudit 6

1. **目的覆盖：PASS**。控制主线和现场取证内容未改变，必须完成的记录动作均有独立播报提示。
2. **步骤必要性：PASS**。只增加新建与保存记录两个必要提示，没有继续扩展其他事件。
3. **现场可执行性：PASS**。只听第一行播报即可知道何时操作、拍照、记录及保存。
4. **时序：PASS**。现场记录在插枪前新建，在拔枪后的断开保持阶段保存，不干扰车辆操作。
5. **分析线索：PASS**。启动和停止方式继续按实际采集秒数记录并与 CAN 文件关联。
6. **内容边界：PASS**。未引入品牌、车型、App 或专属界面路径。

SelfCheck 与 SelfAudit 6 通过，版本 0.6 具备提交 Independent Audit 的条件。本结果不代表 `CONFIRM` 或 `PUBLISH`。

## Adjustment 6

根据 Independent Audit 5 的 Audit Finding 3 修订为版本 0.7，不增加 Event，只补足现有第一行播报：

- 新建记录时直接说明与 CAN 同名；
- 启动和停止时直接提醒记录实际秒数和方式；
- 保存记录时直接说明与 CAN 一起交付。

## SelfAudit 7

1. **目的覆盖：PASS**。第一行播报覆盖现场操作、取证、记录时间、文件关联和交付要求。
2. **步骤必要性：PASS**。没有增加 Event，仅补充完成既有记录闭环必需的词语。
3. **现场可执行性：PASS**。只听第一行即可完成车辆操作和两行现场记录，并知道如何保存交付。
4. **时序：PASS**。新建、填写和保存仍位于原有时点，没有改变车辆控制顺序。
5. **分析线索：PASS**。记录包含实际秒数，通过同名文件与 CAN 关联并一起交付。
6. **内容边界：PASS**。未增加控制分支、车型路径或内部 Signal 推断。

SelfCheck 与 SelfAudit 7 通过，版本 0.7 具备再次提交 Independent Audit 的条件。本结果不代表 `CONFIRM` 或 `PUBLISH`。
